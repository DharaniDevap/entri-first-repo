 let usernames=document.getElementById("usernames")

fetch("https://jsonplaceholder.typicode.com/users")
.then((response)=>{
    return response.json();
})
.then(data=>{
   usernames.innerHTML=""
    for(let i=0; i<data.length; i++){
        usernames.innerHTML+=`<h1>heelo ${data[i].username}</h1>`
    }
});